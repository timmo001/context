---
name: Publish Arch fixture

# yamllint disable-line rule:truthy
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  publish:
    name: Build context-git fixture
    uses: >- # master
      timmo001/workflows/.github/workflows/build-arch-package.yml@bf115c46985ccb2fc320caa5cf8fb257772f6436
    with:
      package-name: context-git
    secrets:
      ARCH_REPO_DISPATCH_TOKEN: ${{ secrets.ARCH_REPO_DISPATCH_TOKEN }}
